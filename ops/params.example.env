# LiteLLM Gateway - Ops 模块参数模板
# 复制为 params.env 后填写（params.env 已被 .gitignore 忽略，勿提交）
#   cp ops/params.example.env ops/params.env

# ---------- IM 通知 ----------
# 群机器人类型：feishu | dingtalk | wecom
IM_TYPE="feishu"
# 群机器人 webhook URL（敏感）
WEBHOOK_URL="https://open.feishu.cn/open-apis/bot/v2/hook/xxxxxxxx"
# 加签密钥（可选：飞书/钉钉支持；企业微信留空）
WEBHOOK_SIGN_SECRET=""

# ---------- WAF ----------
# 管理路径 IP 白名单开关（限制 master key 管理操作）。false 时不创建 IPSet 与锁定规则。
ENABLE_ADMIN_LOCKDOWN="false"
# 管理操作允许的源 IP（逗号分隔 CIDR）——仅 ENABLE_ADMIN_LOCKDOWN=true 时生效
ALLOWED_ADMIN_CIDRS="203.0.113.10/32,198.51.100.0/24"
# ---- 限速（WAF rate-based）----
# 主限速按 API key 聚合（Authorization: Bearer / x-api-key 两种头都覆盖），
# 而非按源 IP——因为大企业 2000+ 员工常共享公司出口 NAT IP，按 IP 限速会误杀整个公司。
#
# RATE_LIMIT_PER_KEY：单个 API key 5 分钟请求上限。
#   建议 = 单个重度用户（如 Claude Code agent）5 分钟峰值请求量 × 3~5。
#   目的：遏制单个被盗/滥用 key，不影响正常用户。
RATE_LIMIT_PER_KEY="3000"
# RATE_LIMIT_PER_IP：单个源 IP 5 分钟请求上限（DoS 兜底 + 覆盖无鉴权头洪泛）。
#   ⚠️ 务必设为「整个公司从单一出口 NAT IP 的 5 分钟峰值请求量」的若干倍，否则误杀正常流量。
#   无 QPS 基线时：先给一个高值（如 20000~50000），部署后看 CloudWatch Dashboard
#   的 RequestCount 基线，1~2 天后再向下收紧。
RATE_LIMIT_PER_IP="20000"
# 区域白名单开关与允许的国家码
ENABLE_GEO_BLOCK="false"
ALLOWED_COUNTRIES="CN"

# ---------- 告警阈值（可选覆盖，默认按 2000+ 员工规模校准）----------
# HTTP_4XX_THRESHOLD：5min 内后端 4XX(401/403/429) 阈值 —— 撞库/凭证滥用信号。
#   ⚠️ 上线 1 周后务必按实际 Target 4XX 基线校准：阈值 = max(历史日峰值 × 3, 200)。
#   否则要么误报(过低)、要么分散式多-key 撞库钻空子(过高)。
HTTP_4XX_THRESHOLD="1000"
# HTTP_5XX_THRESHOLD：5min 内后端 5XX 阈值（LiteLLM/上游报错）
HTTP_5XX_THRESHOLD="25"
# LATENCY_P95_THRESHOLD：p95 响应时间阈值（秒）。LLM 长上下文首字节偏慢，勿设过低
LATENCY_P95_THRESHOLD="45"
# REQUEST_ANOMALY_STDEV：请求量异常检测带宽标准差（越大越不敏感）
REQUEST_ANOMALY_STDEV="3"
# NAT_EGRESS_ANOMALY_STDEV：NAT 出站字节异常检测带宽（越大越不敏感）
#   2000+ 员工爬坡期出站流量增长快，模型基线偏低易误报，默认 6；稳定后可回调
NAT_EGRESS_ANOMALY_STDEV="6"
# MASTER_KEY_USAGE_THRESHOLD：5min 内 master key 调用 LLM API 的请求数阈值（0=只要使用就告警）
#   日常应全部走 virtual key。若现阶段仍有合法 master key 流量，先调高并尽快收敛，收敛后回调 0
MASTER_KEY_USAGE_THRESHOLD="0"

# ---------- 成本告警 ----------
# MONTHLY_BUDGET_USD：月度成本预算（USD）。实际花费超 80% / 预测月底超 100% 各告警一次
MONTHLY_BUDGET_USD="5000"
# COST_ANOMALY_THRESHOLD_USD：费用异常检测的最低异常影响金额（USD）
COST_ANOMALY_THRESHOLD_USD="100"
# EXISTING_ANOMALY_MONITOR_ARN：每账号仅允许一个按服务维度的异常监控器。
#   若账号已有（aws ce get-anomaly-monitors 查询 MonitorType=DIMENSIONAL），必须填入复用，否则部署失败；留空则新建
EXISTING_ANOMALY_MONITOR_ARN=""

# ---------- 可选 ----------
# 目标组全名 targetgroup/xxx/yyy（填了才创建 UnHealthyHost 告警）
# 取值：aws elbv2 describe-target-groups --names litellm-gw-tg \
#        --query 'TargetGroups[0].TargetGroupArn' --output text  的 arn 中 targetgroup/ 之后部分
TARGET_GROUP_FULL_NAME=""
